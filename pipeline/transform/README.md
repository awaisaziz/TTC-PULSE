# Transform

DuckDB verification utilities for transformed parquet output.

## Run verification query

```bash
python -m pipeline.transform.verify_parquet
```

This executes summary checks over `data/parquet/**/*.parquet`.
